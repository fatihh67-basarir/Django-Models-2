# from django.db import models

# Create your models here.
# class Student(models.Model):
#     first_name = models.models.CharField(('Adı'), max_length=50)
#     last_name = models.models.CharField(('Soyadı'), max_length=50)
#     number = models.models.IntegerField(("Numara"))